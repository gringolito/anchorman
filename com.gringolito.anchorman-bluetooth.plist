<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Unique identifier for this daemon -->
    <key>Label</key>
    <string>com.gringolito.anchorman-bluetooth</string>

    <!-- Path to the watcher script -->
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/usr/local/bin/anchorman-bluetooth</string>
    </array>

    <!-- Start automatically at boot -->
    <key>RunAtLoad</key>
    <true/>

    <!-- Restart the daemon if it crashes or exits -->
    <key>KeepAlive</key>
    <true/>

    <!-- Log stdout and stderr -->
    <key>StandardOutPath</key>
    <string>/var/log/anchorman-bluetooth.log</string>
    <key>StandardErrorPath</key>
    <string>/var/log/anchorman-bluetooth.error.log</string>

    <!-- Runs as root; blueutil is invoked as the console user via su -->
    <!-- (LaunchDaemons in /Library/LaunchDaemons already run as root) -->

    <!-- Throttle restart attempts if the script keeps failing -->
    <key>ThrottleInterval</key>
    <integer>10</integer>
</dict>
</plist>
